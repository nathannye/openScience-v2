// import supercomputers from "./supercomputersData";

// let container = document.getElementById("supercomputerHoverContainer");

// for (let i = 1; i < supercomputers.length; i++) {
//   let obj = document.createElement("object");
//   obj.setAttribute("data", `./img/supercomputers/${supercomputers[i].img}`);

//   let fallback = document.createElement("img");
//   fallback.src = obj.data;

//   container.appendChild(obj);
//   obj.appendChild(fallback);
// }
